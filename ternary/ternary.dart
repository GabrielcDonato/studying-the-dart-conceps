void main() {
  int a = 5;
  int b = 10;
  // var result = a > b ? "MAIOR" : "MENOR";
  // se A for maior que B retorna (?) maior senão (:) menor;
  // if A is greater than B returns (?) greater otherwise (:) less;
  var result = a > b ? ["MAIOR"] : ["MENOR"];
  print(result);
}
